# app/services/tts.py

def synthesize_speech(text: str, output_path: str) -> str:
    """
    Convert text to speech and save to output_path (e.g. MP3).
    Return the path.
    """
    # TODO: call your TTS provider here.
    # with open(output_path, "wb") as f: f.write(tts_bytes)
    return output_path
